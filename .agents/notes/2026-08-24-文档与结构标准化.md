# 决策：文档与结构标准化（2026-08-24）

已实施：已实施

## 问题
项目缺少根 AGENTS.md、docs/ARCHITECTURE.md 与子目录双件，文档与代码存在不一致（Python 版本、依赖、结构树缺项）。

## 决策
- 根 AGENTS.md 为维护索引（仪表盘），正文 ≤30 行
- 架构文档统一放 docs/ARCHITECTURE.md，根目录不建独立 ARCHITECTURE.md
- generator/ 与 scripts/ 补齐双件（AGENTS.md 规则层 + README.md 文档层）
- src/、data/、output/、images/、projects/ 属内容/素材/产物目录，不建双件
- README.md 修正 Python 版本（3.12+）与依赖（jinja2 + openpyxl），结构树补齐缺项

## 替代方案（强制）
- 给所有子目录建双件：src/ 是原始素材、output/ 是再生产物，规则层无对象可约束，噪音大于收益
- 根目录放独立 ARCHITECTURE.md：与统一规则冲突（架构文档固定放 docs/ARCHITECTURE.md）
- 重写素材文档（src/*.md、作品集.md 内容）：属用户资产，只修链接，不重写内容

## 影响
- 收益：Agent 进目录即获规则上下文，文档两跳可达，链接可机器校验
- 代价：新增 7 个文档文件，需随代码演进同步维护
- 验证要求：改后跑 check-links.py，报错=目标不存在必须修